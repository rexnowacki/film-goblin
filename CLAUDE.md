# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

> **Convention:** This section is updated before each session close so the next session can pick up cold. Update it at the end of every session — what just shipped, what's next, any open threads worth carrying forward.

**Last updated:** 2026-07-09

**Last shipped (2026-07-09):** **FROM THE PIT — aging / TTL** (`feature/pit-aging-ttl`). System feed events now have a time-to-live on the signed-in `/home` feed, so stale events age out instead of resurfacing when the feed is quiet (the "graveyard of old Pit cards" the owner flagged). New pure module `app/lib/feed-events/pitAge.ts` (14 unit tests): `classifyPitEventAge(createdAt, now)` returns `fresh` (<24h), `aging` (<48h), or `stale` (≥48h) by rolling hours from `created_at`, upper-bound-exclusive (exactly 24.000h is `aging`, exactly 48.000h is `stale`); `filterPitByAge(events, watchlistFilmIds, now)` keeps fresh always, drops stale always, and keeps aging only when the event's film is on the viewer's watchlist (events with `film_id = null` or a non-watchlisted film get a hard 24h shelf life). Constants `PIT_FRESH_HOURS = 24` / `PIT_AGING_HOURS = 48`. Spliced into `getEligiblePitEventsForUser` (`pitSelection.ts`) as a 4-line insertion between the watchlist fetch and the existing watchlist-ranking step — the age-filtered `fresh` array feeds `rankPitCandidatesByWatchlist`, not the raw candidates. **Critical scope boundary:** the filter lives ONLY in `pitSelection.ts` (the signed-in path), NOT in the shared `getRecentSystemEvents` (`query.ts`), which also feeds the anonymous landing page (`getLandingFeed`) — that must stay un-aged, exactly as #1 established. Rolling-hours boundaries (not the UTC-calendar-day used by #1's daily cap, which is a per-day budget rather than a duration). Everything from #1 is untouched: permanent impression exclusion, daily cap, watchlist boost, position rules. The "don't resurface unless status changed" rule needed no new code — a status change is a new `feed_events` row with a new id, which isn't in the permanent `seenEventIds`, so #1's exclusion already satisfies it. Events are never deleted — they persist in `feed_events` forever, so the deferred Ledger (#4) and Pit archive tab (#5) will query them un-aged. Sub-project #2 of 5; remaining backlog: digest events (#3), Ledger page (#4), Pit archive tab (#5). No migration, no new table, no UI change. Spec: `docs/superpowers/specs/2026-07-09-pit-aging-ttl-design.md`, plan: `docs/superpowers/plans/2026-07-09-pit-aging-ttl.md`.

**Previously shipped (2026-07-08, second ship):** **FROM THE PIT — cadence caps** (`feature/pit-cadence-caps`). System feed events on the signed-in `/home` feed are now capped: max 3 new events introduced per user per UTC calendar day, permanent "already seen" exclusion, at most 1 Pit item in the first 6 feed positions, at least 2 user items between any two Pit items (uniform across all tiers, whisper included). A user's own watchlist matches are boosted (+1000 on the existing 10–100 priority scale) so they always win a daily slot ahead of non-matches. First of five sub-projects sketched from the owner's original cadence feedback — aging/TTL visibility, digest events, a Ledger page, and a Pit archive tab remain backlog items, not built here. New `pit_impressions` table (mig 0212, mirrors the existing `fyp_impressions` pattern: SELECT-only RLS for `authenticated`, all writes via `SECURITY DEFINER` RPC `record_pit_impressions`, `ON CONFLICT DO NOTHING` — presence-only, no repeat counter, batch cap 10). New pure module `app/lib/feed-events/pitPosition.ts` (`enforcePitPositionRules`, 9 unit tests) runs between `composeFeed` and `resolvePitTiers` (both untouched) — drops position-rule violators rather than reordering them, so a dropped item consumes neither the daily budget (no impression recorded) nor `resolvePitTiers`' full-card window. New `app/lib/feed-events/pitSelection.ts` (`getEligiblePitEventsForUser` + `rankPitCandidatesByWatchlist`, 6 pure unit tests + 4 integration tests) does permanent exclusion, the daily-cap early-exit, and the watchlist boost before candidates ever reach `composeFeed`. Impressions recorded client-side via `recordPitImpressions` (mirrors `recordFypImpressions` exactly) on actual `SystemEventRow` mount, every tier. Signed-in `/home` only — the anonymous landing page (`LandingFeedCard`/`getLandingFeed`) is untouched, and signed-out `/home` visitors keep today's uncapped behavior. UTC calendar day boundary (no per-user timezone tracking exists anywhere in this app). **Two things caught during implementation, both fixed:** an interrupted fix-subagent's first draft of the cross-day-permanence test used `PIT_DAILY_CAP` fresh impressions as its differentiator, which returns the same result under both correct and buggy behavior — tightened to `PIT_DAILY_CAP - 1`, the real boundary, independently re-verified by hand-trace. And `types.ts` needed a `Functions.record_pit_impressions` entry (not just the `Tables.pit_impressions` entry the migration task covered) for `client.rpc(...)` to typecheck — matches the existing `record_fyp_impressions` precedent exactly. **Also fixed this session, unrelated to this ship:** `docs/sub-project-history.md` rows 39/40 had gotten inserted out of chronological order in an earlier same-session edit (a copy-paste insertion-point mistake) — swapped back to the correct order. Manual dev-server smoke deferred to the ship sequence below (migration not live anywhere reachable until then). Spec: `docs/superpowers/specs/2026-07-08-pit-cadence-caps-design.md`, plan: `docs/superpowers/plans/2026-07-08-pit-cadence-caps.md`.

**Previously shipped (2026-07-08, first ship):** **FROM THE PIT — three-tier feed treatment + seal avatar** (`feature/pit-tier-system`). System feed events now render at one of three tiers derived purely from event type — never chosen per-post — via `getPitTier`/`getPitKicker`/`getPitPriceVars`/`getPitBadges` in new `app/lib/feed-events/tier.ts` (11 unit tests): **whisper** (no avatar, magenta left-border, dimmed cream copy — ambient/low-signal types like `price_rise`, `anniversary`, `milestone`, `full_moon`, `monthly_communion`), **standard** (40px seal, plum wash background, source/FREE/price badges via `getPitBadges` — most types, e.g. `price_drop`→"TITHE LOWERED", `now_free`→"NO TITHE"), **full** (solid plum card, price ledger chip showing old→new — reserved for `all_time_low` only, the one type with genuine before/after price data). A new feed-level pass, `resolvePitTiers` in `app/lib/feed-events/pitCadence.ts` (8 unit tests), caps full cards at one per any 8-consecutive-feed-item sliding window (`PIT_FULL_CARD_WINDOW`), demoting (never dropping) excess candidates to standard — runs once after `composeFeed` (itself untouched) on the final interleaved array, counting every feed item toward the gap, not just system rows. New seal avatar (`PitSeal` in `systemEventParts.tsx`, replacing the "FG" SVG `PitSigil` from earlier today) renders `app/public/pit-seal.png` at a flat 40px on `/home` (`SystemEventRow`) and 32px on the anon landing card (`LandingFeedCard`). **Real artwork landed same-day** (`feature/pit-seal-artwork`): the owner's supplied art had a solid black (no-alpha) background, knocked out to transparency via a color-distance threshold against the sampled background (distinct enough from the plum interior's own dark purple to cut cleanly, verified no fringing at the wax perimeter), served at 160×160 RGBA; full-res original kept at `pit-seal-source.png` matching the `icon-source.png` convention. Fixed (non-theme-switched) `--pit-*` tokens added to `00-core.css`'s base `:root` — deliberately never theme-scoped, since the Pit is the product's own voice, distinct from the viewer's chosen `--accent` theme. Four copy templates rewritten in `copy.ts` for a new voice rule (goblin ACTION verb first, never a goblin FEELING — forward-only, stored copy is frozen and untouched). `LandingFeedRow`'s `"system"` variant widened from `{copy, film}` to `{event: SystemFeedEvent}` (the smallest change needed to make tier resolution possible on the landing page at all — only `LandingFeedCard.tsx` and its test consumed the old shape). **Two real gaps surfaced by typecheck, not in the original plan, both fixed:** `FeedRow.tsx` is a second, currently-unreachable `SystemEventRow` consumer that also needed a tier prop (falls back to `getPitTier`'s natural tier — confirmed dead code today, since `FeedTabs` calls `SystemEventRow` directly and `FollowedActivityFeed`, `FeedRow`'s only other caller, never produces system items); `FeedTabs`'s `composed` array had already collapsed back to `FeedItem` shape before `resolvePitTiers` could consume it, split into `composedRaw` (feeds the tier resolver) + `composed` (unchanged for every other consumer). A code-review finding on the cadence window's boundary math was investigated and found to be a false alarm — the shipped `<8` comparison matches the exact algorithm approved earlier in the design conversation (minimum gap of 8 between full cards); a tighter boundary test was added for future confidence, no code changed. **Manual verification gap:** no browser extension was available in this environment, so the Definition of Done's required mobile screenshot was not captured — verified correctness instead via direct HTML inspection of a live dev server (6 real standard-tier rows rendered correctly with real kickers after clearing a stale `unstable_cache` entry that had been serving pre-migration `{copy,film}`-shaped data and crashing `resolvePitTiers` on `undefined.event_type` — self-heals within the 5-min TTL on a real deploy, since Vercel's build-ID change busts the persistent cache anyway). Spec: `docs/superpowers/specs/2026-07-07-pit-tier-system-design.md`, plan: `docs/superpowers/plans/2026-07-07-pit-tier-system.md`.

**Previously shipped (2026-07-07, second ship):** **The Claiming — buy-click capture & purchase confirmation** (`feature/buy-claim-loop`). Clicking a Buy on Apple TV link (film page or watchlist caption) now arms a client-side pending-buy queue (`fg_pending_buys` in `localStorage`, via `app/lib/purchase/pending.ts` — pure functions over an injected storage interface, 10 unit tests). New `BuyOnAppleLink` client component wraps both buy surfaces and records `{filmId, title, price, clickedAt}` on click for signed-in users only (`addPendingBuy`); navigation is never blocked. A `PurchasePrompt` modal, mounted once in the signed-in app layout, checks the queue on mount and on tab refocus (`visibilitychange`) and raises a BottomSheet once the entry is 2 minutes–48 hours old ("Did you claim **{title}**?"). **Claimed it** calls the new `confirmPurchase` server action (`app/lib/actions/library.ts`), which fills `library.price_paid_usd` (mig 0211, nullable `NUMERIC(6,2)`) — fresh grimoire adds store the price and drop any watchlist row (owning supersedes wanting, same as `_addToLibrary`); an already-owned film only has a NULL price filled, never overwritten — then shows a reward line with savings vs. the film's all-time `price_history` peak. **Not this time** clears the entry; dismissing without answering defers once, then drops. `/library` gains a stat strip (Claimed / Tithed to Apple / Kept from the fire) via `getLibrarySavings`, computed at read time from `price_history` peaks — savings are never stored. **RLS gotcha caught in code review:** `library` had only ever had SELECT/INSERT/DELETE policies since mig 0122 — the fill-null-price UPDATE path would have thrown permission-denied. Mig 0211 was amended (before ever being applied) to add an owner-only `library_update` policy + `GRANT UPDATE`. Accepted deliberately: `price_paid_usd` is coven-visible alongside the rest of the row (no column-level guard). Deferred: cross-device pending prompts (would need a server-side table), public-profile savings display, editing/backfilling price on existing rows. **Rollout order: migration 0211 first, then deploy — higher stakes than the usual "only new code reads the column" pattern.** `/library`'s page load unconditionally calls `getLibrarySavings`, which selects `price_paid_usd`; deploying before the migration doesn't just leave the new feature inert, it 500s the *existing* `/library` page for every signed-in user. Spec: `docs/superpowers/specs/2026-07-07-buy-claim-loop-design.md`, plan: `docs/superpowers/plans/2026-07-07-buy-claim-loop.md`.

**Previously shipped (2026-07-07, first ship):** **Film page price sticker** (`feature/film-page-price-sticker`). The current price on `/film/[id]` now renders as a die-cut vinyl sticker (accent/void, rotate(-6deg), 2px radius — OG-card visual language) in the hero text column above the action cluster, with one context beat from the 180-day history (`pickPriceBeat` in `app/lib/price-beat.ts`: lowest-in-window → ▼ down-from → plain "on Apple TV"). Informational only — Buy button stays the sole CTA, its label simplified to `Buy on Apple TV →` (price no longer duplicated). Mounts only when `itunes_url && currentPrice != null`; never on the poster (PR #51 settled decision holds). New `app/components/FilmPriceSticker.tsx` (server-rendered, no "use client"). No migrations, no new queries. Spec: `docs/superpowers/specs/2026-07-07-film-page-price-sticker-design.md`, plan: `docs/superpowers/plans/2026-07-07-film-page-price-sticker.md`.

**Previously shipped (2026-07-06, second ship):** **Living Pit v2 — eight new feed events** (`feature/living-pit-v2`, PR #185, squash-merged as `dbb8135` + migrated + deployed + smoked; **+ hotfix PR #187** — the anon landing (`/`) threw a server exception, digest 2199110839, the moment a system row reached `LandingFeedCard`: it called `renderCopyText` imported from the `"use client"` `SystemEventRow` module, an illegal client-reference invocation server-side. Latent since #180; v2's fresh Monday rows first triggered it. `renderCopyText`+`PitSigil` now live in the shared non-client `components/activity/systemEventParts.tsx` — helpers a server component must CALL can never be exported from a `"use client"` file. Lesson: smoke the signed-out landing, not just `/home`, after feed-event changes). Event census is now 15 types: v1's seven plus `left_free`, `now_free`, `now_on_apple`, `last_showing`, `verdict_anointed`, `now_at_theater`, `full_moon`, `monthly_communion`, and a summon copy variant for `new_film` (`fulfillFilmRequest` passes `summoned: true` through `adminCreateFilm`; the override in `renderCopy` ignores variant rotation). All v1 plumbing (emit dedup/variants, composer, FROM THE PIT rendering) reused unchanged — mig 0210 just extends the enum. Emission map: daily job (`daily.ts`) gained `last_showing` (Loft finale within next 24h — **deliberate deviation from the plan's UTC-midnight SQL**, which was near-inert for Tucson evening finales; commit `f1c9a04`), `verdict_anointed` (≥90%/≥5, once-ever via NOT EXISTS), `full_moon` (pure `moon.ts` mean-synodic check ±12h; werewolves-first pool, creature-trio fallback, 3-day global lookback, rotation by fewest prior appearances), `monthly_communion` (1st of month, most-watched of prior month, payload-month dedup); streaming refresh diffs free providers (`flatrate|free|ads`) before/after `replaceProviders` → `now_free`/`left_free`; Loft scrape emits `now_at_theater` post-scrape (weekly dedup absorbs Monday re-runs); the three `itunes_id` graft points (twin promote, cron auto-promote, candidate approve) emit `now_on_apple` via `emitFeedEventSvc` (svc writer is restricted to `now_at_theater`/`now_on_apple`/v1's `new_film`+`goblin_pick` — payload-dedup types must use the pg flavor). Priorities: left_free=88, now_free=85, now_on_apple=82, last_showing=78, verdict_anointed=75, now_at_theater=65, full_moon=45, monthly_communion=40. Testing note: `promote-tmdb-twin.test.ts` now mocks `@/lib/supabase/service-role` — the emission's fresh svc client (correct in source: the caller injects a user-scoped client) was making an unmocked live write attempt in tests. Smoke (Monday 2026-07-06): 2× `now_free` + 1× `now_at_theater` landed in `feed_events`; `dailyFeedEvents: 0` honest (no Anointed-eligible films yet, not the 1st, no full moon). Accepted minors: `summoned` sits in `payload.vars` not `payloadExtra`; "The Loft" hardcoded single-theater assumption in two queries; failed provider snapshot degrades to possible false `now_free`. Spec: `docs/superpowers/specs/2026-07-06-living-pit-v2-design.md`, plan: `docs/superpowers/plans/2026-07-06-living-pit-v2.md`.

**Previously shipped (2026-07-06):** **The Living Pit — system feed events** (`feature/living-pit-feed-events`, PR #180 + hotfix #181, merged + migrated + deployed + smoked). Automated system events now interleave with user activity on `/home` (the "all" tab) and the anon landing card, so the feed never reads as empty. Core 7 event types in v1: `price_drop`, `all_time_low`, `price_rise`, `new_film`, `anniversary`, `goblin_pick`, `milestone` (deferred: `now_free`/`left_free` — needs provider snapshot/diff; `ritual_open`/`badge_earned` — needs rituals; `death_day` — needs person dates). Architecture: system-only `feed_events` table (mig 0209, anon+authenticated SELECT, service-role-only writes) merged with the untouched `activity` machinery at read time by the pure composer `app/lib/feed-events/compose.ts` (2:1 ratio cap vs user activity, ≤6/day when user activity is zero, no same-type stacking, priority selection, date-seeded determinism — composition happens client-side in `FeedTabs` with a server-computed daily seed). Copy is rendered at creation (`copy.ts`, goblin-voiced templates with variant rotation stored in payload) and frozen in the row. Generators: post-sweep price scan (`price-scan.ts`, advisory lock 40712027, derives events from `price_history` — **column is `captured_at`**, the created_at assumption was the #181 hotfix) + daily anniversary/milestone job (`daily.ts`), both jobs in the maintenance cron (`jobs.priceFeedScan`, `jobs.dailyFeedEvents`) and the scan also in `/api/cron/refresh-prices` (failure-isolated — a scan error degrades to `feedScan:{error}` without breaking the price digest). `new_film` emits from `adminCreateFilm` (fresh-insert path only, never twin promotion; bulk-films inherits via the wrapper); `goblin_pick` from `scheduleGoblinPick`. Dedup: 7 days per (film, type) in `emit.ts`; milestones once-ever per kind+n; ATL deletes a same-day drop. One-time TMDB release-date backfill ran against prod (243/243 films updated — anniversaries now have real supply). Smoke verified: milestones (250/300 catalog, member 15) + a real `price_rise` (It Comes At Night) landed in `feed_events`. Same-day amendment (PR #183): emoji-led styling vetoed — system rows now share user-row anatomy (⛧ sigil in the avatar slot, small-caps FROM THE PIT tag + relative time in the footer); leading emoji dropped from all copy templates, old rows groomed at render by `stripLeadingEmoji`. Spec: `docs/superpowers/specs/2026-07-05-living-pit-feed-events-design.md` (see 2026-07-06 amendment), plan: `docs/superpowers/plans/2026-07-05-living-pit-feed-events.md`.

**Previously shipped (2026-07-05):** **Web push notifications** (`feature/web-push-notifications`, PR #178, merged + deployed + migrated). Social kinds + price drops now go out as Web Push to subscribed devices, opt-in via a single "Push notifications" toggle on `/settings` (`#push`, `PushToggle` component; handles unsupported/denied states with an iOS Home-Screen hint). Pipeline: `AFTER INSERT` trigger on `notifications` (mig 0208) fires an async `pg_net` POST to `/api/push/fanout` with `Authorization: Bearer <PUSH_FANOUT_SECRET>`; the route filters by the v1 kind allowlist (`PUSH_KINDS` in `app/lib/push/payload.ts`: coven invites/accepts, recommendations, comments/replies, gazing RSVPs, price drops), builds a goblin-voiced payload ("Apple TV" naming), and sends via `web-push` to every `push_subscriptions` row for that user, pruning dead endpoints (404/410). Mig 0208 adds `push_subscriptions` (owner-scoped RLS), `push_fanout_config` (single-row, service-role only — holds fanout URL + secret; row inserted in prod), and the trigger (`SET search_path = public, extensions, net` — same pgcrypto lesson as mig 0176). Minimal service worker at `app/public/sw.js` (display + deep-link only, NO caching by design). Server actions in `app/lib/actions/push.ts`: on resubscribe the delete-by-endpoint runs via service role (endpoint is UNIQUE; a device switching accounts must clear the previous owner's row). Four new env vars in Vercel prod + `app/.env.local`: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY` (sensitive), `VAPID_SUBJECT`, `PUSH_FANOUT_SECRET` (sensitive). Note: the real secondary-button class is `.btn-outline` (the `.btn-outline-bone` name in `app/components/CLAUDE.md` is conceptual). **Device smoke test still owed** (plan Task 7 Step 5): enable push on the owner's iPhone Home-Screen install + desktop Chrome, have moss.witch send a recommendation, confirm delivery + deep link. Spec: `docs/superpowers/specs/2026-07-03-web-push-notifications-design.md`, plan: `docs/superpowers/plans/2026-07-03-web-push-notifications.md`.

**Previously shipped (2026-07-03):** **FYP poster quick-add** (`feature/fyp-poster-quick-add`). The Browse tab's `PosterQuickAdd` affordance (desktop hover + menu; mobile ⋯ bottom sheet: log a watch / watchlist / grimoire / recommend / share) is now on the For You tab too — shelf posters (`ShelfCarousel`) and the Daily Omen hero (`DailyOmenHero`). The "Not interested" dismiss lives inside the quick-add menus (desktop pill + mobile sheet row via optional `onNotInterested` prop on `PosterQuickAdd`; the corner ✕ shipped first, owner vetoed it same day — see spec amendment); `.poster-quick-add .match-pill { right: 44px }` keeps the band pill clear of the ⋯ button. `ForYouSection` in `app/app/films/page.tsx` fetches the viewer's `watchlists`/`library` film-id sets + username in parallel for honest initial ✓ state. Same-day follow-up (PR #172): **FYP now hard-excludes saved films** — `ScoreContext.userSavedFilmIds` (watchlist ∪ library) joins watched/disliked/dismissed as the fourth exclusion, in both the scored feed and the cold-start starter pack; saves still feed the affinity vector. This makes the honest-✓ plumbing mostly dormant (kept — still correct for mid-session adds). No migrations, no new actions across all three PRs (#170–#172). **Manual UI pass still owed** — checklist in plan Task 3 (`docs/superpowers/plans/2026-07-03-fyp-poster-quick-add.md`), adjusted for the amendments: mobile pill/⋯ spacing, "Not interested" via the ⋯ sheet and desktop hover menu (no corner ✕ anymore), and that saved films no longer appear on shelves. Spec: `docs/superpowers/specs/2026-07-03-fyp-poster-quick-add-design.md`. Fourth same-day ship: **OG share card redesign** (`feature/og-poster-card`) — `/api/og/film/[id]` is now a portrait 1000×1500 poster-dominant card (the landscape text card was unreadable at iMessage bubble scale): full-bleed artwork, gradient brand band, rotated pink price sticker when `films_with_stats.available && latest_price` ("on Apple TV" naming). Price-bearing cards cache 1h vs 24h (weekly price cron staleness); no-artwork films get a same-size portrait text card; `generateMetadata` on `/film/[id]` declares 1000×1500. Landscape unfurlers (X large card) will crop — accepted trade for a texting-first share flow. Fifth ship (PR #175): **film-page polish pass** — priced Buy CTA as the sole solid-accent button (Share/Recommend demoted to outline), 4-line description clamp (`FilmDescription`), stepped price sparkline under `PriceStatBlock` (kept subordinate — the old full chart was deliberately replaced, see 10-price-stat.css header), WatchProviders single-group label dedupe, verdict band collapses to one column, empty Editorial Reviews section hidden + grid made stackable.

**Previously shipped (2026-07-01):** Two things landed the same day. (1) **iTunes transition hardening** (`feature/itunes-transition-hardening`). The weekly iTunes availability check (Mondays inside the daily maintenance cron) had silently stopped finding anything: Apple's Search API returns zero results whenever `entity=movie` is set, and the scorer couldn't match Apple's "Title (YYYY)" naming. Fixes: `itunes-search.ts` drops the entity filter, searches wide (limit 50), filters `kind === "feature-movie"` client-side; `score.ts` strips a trailing `(YYYY)` suffix from both sides before comparing (never a bare-year title like "1917"); `check.ts` gains a Brave → Apple TV page → adamId → iTunes Lookup fallback (same pipeline as the manual admin add) when iTunes search yields nothing viable — needs `BRAVE_SEARCH_API_KEY`, fails soft to []; `adminCreateFilm` promotes an existing TMDB-only twin in place (graft iTunes identity via `lib/admin/promote-tmdb-twin.ts`) instead of inserting a duplicate. Also merged the duplicate Obsession rows in prod (kept TMDB row `b4ed5896…` with all user data, grafted `itunes_id 1895945921`, moved price_history, deleted the Apple TV twin). (2) **FYP Discover Shelves v3.5** (`feature/fyp-discover-shelves`). The FYP recommender is re-exposed as the default "For You" tab on `/films` (Discover is now a two-tab shell: For You default for signed-in users, Browse All preserves the old catalog UI — `?tab=browse`/`?q`/`?sort`/`?page` select Browse). The flat rank-offset `getForYou` feed and `/for-you` route are gone; `/for-you` permanently redirects to `/films`. New orchestrator `getForYouShelves` (in `forYou.ts`) assembles themed shelves (Hexed for You, Because you loved X, Coven Favorites, New to the Pit, Strange Pulls, plus a seeded Daily Omen) via the new pure module `app/lib/queries/fyp/shelves.ts`. Two new feedback tables close the loop: `fyp_impressions` (mig 0206, written via `record_fyp_impressions` RPC) feeds impression-fatigue damping in the scorer (`FATIGUE_FREE_IMPRESSIONS=3`, `FATIGUE_K=0.15`, `FATIGUE_FLOOR=0.35`), and `fyp_not_interested` (mig 0207) both hard-excludes dismissed films and feeds aversion at `SIGNAL_WEIGHTS.not_interested = -1.5`. New actions in `actions/fyp.ts`: `recordFypImpressions` (fire-and-forget via an impression-queue helper), `setNotInterested`/`undoNotInterested`. **Rollout order matters:** migrations (0206, 0207) first, then deploy — the app code is the only consumer of the new tables, so applying migrations first is safe. Spec: `docs/superpowers/specs/2026-07-01-fyp-discover-shelves-design.md`.

**Previously shipped (2026-06-09):** Tier-1 security hardening branch in progress. Migs 0203–0205 add profile column-level grants, pre-auth subject/IP rate limits, and DB CHECK constraints for profile text fields and watched notes. App changes remove client-role `profiles.select("*")`, add auth throttles with fail-open pre-auth limiter helpers, raise new-password minimum to 8, consolidate username validation, and add friendly profile length validation. **Rollout order matters:** deploy app first, then apply migrations; running 0203 before the app deploy can break old `profiles.select("*")` code.

**Last shipped (2026-05-25):** Sub-CLAUDE.md files added throughout the repo to give Claude institutional context at each layer without re-reading root CLAUDE.md. Files at: `app/CLAUDE.md`, `app/components/CLAUDE.md`, `app/lib/actions/CLAUDE.md`, `app/lib/queries/CLAUDE.md`, `app/lib/queries/fyp/CLAUDE.md`, `app/lib/supabase/CLAUDE.md`, `app/lib/theaters/CLAUDE.md`, `db/CLAUDE.md`, `db/migrations/CLAUDE.md`, `worker/CLAUDE.md`, `notifier/CLAUDE.md`.

**Last shipped (2026-05-08):** Four things. (1) **iTunes availability cron (#41):** Weekly cron at `/api/cron/check-itunes-availability` (Mondays 14:00 UTC) checks TMDB-only films for iTunes availability. Mig 0175 adds `films.tmdb_id`, `films.theatrical_release_date`, `films.last_itunes_check_at`, restores partial unique index on `films.itunes_id`, and creates `itunes_candidates` table. Score function in `app/lib/itunes-availability/score.ts` (lowercase-only/normalized title + year ±1 + director); ≥0.85 auto-promotes (writes `itunes_id` directly to films), 0.45–0.85 lands in `/admin/itunes-candidates` for review. Add Film modal (TMDB option) now captures `tmdb_id` + `theatrical_release_date` so new films get precise cron timing; older TMDB-only films use `year ≥ currentYear-1` fallback. 30-day post-theatrical threshold, 365-day cap, 6-day re-check cooldown, 14-day rejection cooldown. Spec at `docs/superpowers/specs/2026-05-08-itunes-availability-cron-design.md`. (2) **Force-change-password admin flow:** Admin sets a temp password from `/admin/users/[id]` → `must_change_password` flag (mig 0174) flips → middleware redirects flagged users to `/auth/change-password` until they pick a new one. Sign-out escape hatch on the change-password page. Coexists with the existing email-based reset (which only works for real-email users; synthetic-email signups can't receive email). (3) **Invite cookie bug fix:** `/invite/[code]` was a Server Component that couldn't write cookies — every redeemed invite was silently rejected at signup. Converted to a Route Handler; expired UI moved to its own `/invite/expired` page. (4) **Mig 0176 — invite trigger search_path fix:** Mig 0173 had pinned `create_invite_code_for_new_user()` to `SET search_path = public`, which broke `gen_random_bytes(8)` resolution (pgcrypto lives in the `extensions` schema in Supabase). Every signup hit "Database error creating new user" via the auth API. Fix widens search path to `public, extensions`. Verified end-to-end against prod via direct admin createUser. **All signups (invite-gated and otherwise) work again.**

**Previously shipped (2026-05-07):** Five things. (1) **Invite codes / cold-start gate (#40):** Hard signup gate behind multi-use invite codes (5 per code). `invite_codes` + `invite_uses` tables (mig 0172 + 0173 fixes). DB trigger auto-creates one code per user on profiles INSERT. `burn_invite_code` PL/pgSQL RPC for race-safe increment. Cookie bridge: `/invite/[code]` validates → sets `fg_invite_code` cookie (24h) → redirects to `/auth/signup`. Gate in `signUp` action (`INVITE_GATE=1` env flag — delete to open signup, no code change). Settings page: "Your Invite Link" block with copy button + usage counter. Admin panel at `/admin/invite-codes` — create batch codes, revoke, usage table. Gate shipped live, **since disabled** (env var removed; set `INVITE_GATE=1` to re-arm). (2) **og:image for film pages:** `/api/og/film/[id]` route via `ImageResponse` — poster left, title/meta/description right, Film Goblin branding. `generateMetadata` on `/film/[id]` now uses the OG URL. (3) **Delete account:** `deleteAccount()` server action + `DeleteAccountSection` in settings — username-confirm gate before hard-delete via service-role. (4) **Film request / Summon (#39):** "Summon it →" on empty discover state → `FilmRequestSheet` multi-step flow. `film_requests` + `film_request_users` tables (mig 0170, 0171). Admin queue at `/admin/film-requests`. (5) **Admin password reset + feed tab fixes + watchlist search + admin last-activity** (see prior session for details).

**Previously shipped (2026-05-05):** Sticky sidebars on /home (`LedgerPanel` left, `GoblinRecommends` right); `goblin_pick` table (mig 0164) + `/admin/goblin-pick`; Add Film modal (admin-only, `UserMenu`); merged `feature/local-theater-alerts`; `FollowedActivityFeed` rewritten to use `groupFeed` + `FeedRow`.

For full history → `docs/sub-project-history.md`.

**Next up:** Nothing queued. **Invite gate is disabled — signup is open** (re-enable by setting `INVITE_GATE=1` in Vercel env). Adjacent quick wins: comment editing + pagination (still missing — no update action exists); genre filter on `/films` (still unwired). *(Stale items removed 2026-07-01: `/lists/[id]` shipped 2026-05-25; trailer UI shipped — `TrailerButton` on `/film/[id]`; handle validation consolidated into `app/lib/auth/username.ts`, used by both onboarding and settings.)*

**Open threads:**
- **FROM THE PIT cadence caps — device smoke test owed.** Verified by unit/integration tests and code review only — the actual signed-in `/home` experience (first-screen cap, min-gap spacing, daily budget persisting across reloads, watchlist boost actually surfacing a match) has not been watched with real eyes. Runbook: sign in, load `/home`, confirm at most 1 Pit item in the first 6 rows and no two Pit items closer than 2 user rows apart; reload several times same-day and confirm the same events keep appearing (not reshuffled); if you have a watchlist match with a pending Pit event, confirm it wins a slot over higher-priority non-matches. Also confirm signed-out `/home` and the anon landing page are both unaffected (regression check). **Aging/TTL (#2) folds into this same pass:** confirm a Pit event older than 48h never appears in the main feed, and that a 24–48h-old event appears only when its film is on your watchlist. Both are hard to force naturally, so the reliable check is a direct DB spot-check (backdate a `feed_events.created_at`, then load `/home`) rather than waiting for real events to age.
- **FROM THE PIT — three more sub-projects sketched, not built.** The owner's original cadence feedback described five pieces; cadence caps (#1) and aging/TTL (#2) have shipped. Remaining, still just ideas: digest events (#3 — bundle same-kind occurrences into one card instead of a wall of individual posts), a dedicated "Ledger" page (#4 — personal price-tracking utility), and a "Pit tab" (#5 — the full unfiltered catalog-activity archive; also the surface that will show the 48h+ events aging/TTL now drops from the main feed but still keeps in `feed_events`). See `docs/superpowers/specs/2026-07-08-pit-cadence-caps-design.md`'s Out of Scope section for the full breakdown.
- **FROM THE PIT — mobile screenshot still owed.** The Definition of Done called for a screenshot showing all three tiers interleaved with user rows on a real device/viewport; no browser extension was available in the implementing session, so this was never captured. Correctness was verified instead via direct dev-server HTML inspection (confirmed real `standard`-tier rows render with correct kickers) and the exhaustive unit test suites for `tier.ts`/`pitCadence.ts` — but nobody has actually looked at this with their eyes yet. Take one on `/home`'s "all" tab once a few days of real events (including, ideally, an `all_time_low`) have accumulated to show all three tiers together.
- **The Claiming — device smoke test owed.** The full click→return→confirm loop has only been verified by unit tests and a hand-trace review (the RLS gap review caught was fixed pre-merge, but the actual PurchasePrompt UI, BuyOnAppleLink capture, and confirmPurchase against a real DB have not been exercised end-to-end). Runbook: click Buy on a priced film's page (or the watchlist caption link) while signed in, wait 2+ minutes, refocus the tab (or reload), confirm the modal appears with the right title/price, tap "Claimed it," confirm the reward line and that `/library`'s stat strip updates. Repeat on the iPhone PWA — `visibilitychange` behavior there is unverified (buy in the Apple TV app, return to the standalone PWA). If the modal never appears, check `localStorage.fg_pending_buys` in DevTools first (queue not writing) before suspecting the modal logic.
- **Living Pit v1+v2 generators — watch for a week.** `jobs.priceFeedScan` and `jobs.dailyFeedEvents` in the maintenance cron output; events land in `feed_events`. v2-specific watch items: (1) the streaming-refresh `now_free` diff — cold-start false positives possible for never-refreshed films (first Monday fired only 2, sane); (2) `verdict_anointed` fires the day the first film crosses ≥90%/≥5 — a multi-film backfill day should stay within the composer's 6/day cap; (3) `now_on_apple` live-fires on the next candidate promotion (Backrooms expected via Monday cron) — verify then; (4) `monthly_communion` first fires 2026-08-01; (5) next `full_moon` window ~2026-07-29 — pool prefers `werewolves` tag (census: 1 film) with vampire/zombie/kaiju fallback. Still deferred: `death_day` (needs person dates), ritual events, per-user feeds. Review minors in the PR #185 body; working ledger was `.superpowers/sdd/progress.md` (untracked).
- **Web push device smoke test owed.** Route auth verified (`curl -X POST https://film-goblin.vercel.app/api/push/fanout` → 401), migration + config row applied — but no real push has been delivered to a device yet. Runbook: plan Task 7 Step 5. If nothing arrives, check `SELECT * FROM net._http_response ORDER BY created DESC LIMIT 5;` — a 401 there means the `push_fanout_config` secret mismatches Vercel's `PUSH_FANOUT_SECRET`; no rows means the trigger didn't fire.
- **Push secret rotation.** Rotating VAPID keys invalidates ALL push subscriptions — every user must re-toggle push in Settings; don't rotate casually. `PUSH_FANOUT_SECRET` rotation: regenerate (`openssl rand -base64 32`) → update Vercel env + the `push_fanout_config` row + `app/.env.local` → redeploy.
- **`CRON_SECRET` rotation procedure.** Vercel marks it "Sensitive" — value can't be viewed after creation. If crons go silent with no visible logs, suspect the secret first (Hobby log retention is ~1h). Local copy at `.cron-secret` (gitignored). Rotation: `openssl rand -base64 32` → save to `.cron-secret` → paste into Vercel dashboard → redeploy from repo root → smoke with `curl -H "Authorization: Bearer $(cat .cron-secret)" https://film-goblin.vercel.app/api/cron/refresh-prices` (expect `{"ok":true,"digest":{...}}`).
- **iTunes availability check runs Mondays inside the daily maintenance cron** (`/api/cron/maintenance`, 10:00 UTC daily — the standalone schedule was dropped for the Hobby cap, but the maintenance fold-in covers it). Manual smoke: `curl -H "Authorization: Bearer $CRON_SECRET" https://film-goblin.vercel.app/api/cron/check-itunes-availability`. Review candidates at `/admin/itunes-candidates`. Rejection cooldown is 14 days. Uses Brave fallback when iTunes search misses (needs `BRAVE_SEARCH_API_KEY`).
- **Rate reminders run daily inside the maintenance cron** (`jobs.rateReminders`). Manual smoke: `curl -H "Authorization: Bearer $CRON_SECRET" https://film-goblin.vercel.app/api/cron/send-rate-reminders`.
- **Theater alerts run Mondays + Thursdays inside the maintenance cron** (`jobs.theaterAlerts`, lock-guarded). Manual smoke: `curl -H "Authorization: Bearer $CRON_SECRET" https://film-goblin.vercel.app/api/cron/theater-alerts`. Details: `app/lib/theaters/CLAUDE.md`.
- **Showtimes refresh** — runs inside the daily maintenance cron on Mondays. Manual smoke endpoint: `curl -H "Authorization: Bearer $CRON_SECRET" https://film-goblin.vercel.app/api/cron/refresh-showtimes`. Scrapes Loft next-7-days showtimes into `theater_showtimes`; powers the "Now at The Loft" pill on `/film/[id]` and the `/gazing/[token]` share. Details: `docs/superpowers/specs/2026-06-03-loft-showtimes-shared-gazing-design.md`.
- **3 films untagged** — Materialists, Smashing Machine (catalog stragglers — could delete), The Surrender (needs manual review).
- **Recommender v3.5 unverified at user scale.** Watch the For You tab on `/films`; tuning constants (including v3.5 fatigue levers) in `app/lib/queries/fyp/CLAUDE.md`. Feedback instrumentation now in place — impressions and "not interested" dismissals are recorded (`fyp_impressions`, `fyp_not_interested`) and feed back into scoring, so this is a good moment to start watching real usage signal rather than guessing.
- **`feature/fyp-discover-shelves` rollout order:** migrations (0206, 0207) first, then app deploy — same pattern as the tier-1 security branch, but here the direction doesn't matter for safety since only new code reads the new tables. Migrate first anyway to keep the convention consistent.
- **`fg-trailers/`** — `cargo run --release` from `fg-trailers/`. Needs `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in `fg-trailers/.env`. Trailer columns (mig 0150) are surfaced by `TrailerButton` on `/film/[id]`; TMDB enrichment also populates them on film create.
- **`passwords.txt`** at repo root (gitignored) — Supabase prod pooler URL + password. Second machine: `npx vercel env pull app/.env.local --yes --environment=production`.
- **Supabase "Confirm email" toggle stays ON.** Synthetic-email signups bypass via `email_confirm: true`. Don't flip it off.
- **Supabase dashboard hardening (manual, post-merge):** set Auth minimum password length to 8 and enable leaked-password protection if the plan allows.
- **`onboarded_at` backfill:** To force a user through onboarding again, null it directly in DB.
- **Genre filter on `/films` is unwired.** `films.genre_primary` exists but no UI reads it.
- **Sub-project #25 deferred:** comment editing/pagination/@-mentions/markdown; emoji quick-react strip.
- **Sub-project #28:** AvatarEditor `react-easy-crop` overlay against `#141414` may read dim — unverified.
- **Sub-project #29:** Chips + textarea in RecommendModal may push composer off-screen on small phones with keyboard up.
- **B2 deferred:** coven-scoped signals on `/p/[username]`, owned + review badges, most-watched sort on `/films`. Coven rating pills on poster grids is the fast follow-up.
- **`PosterQuickAdd` `initialOnWatchlist`** — do NOT drop it. Search mode lifts the watchlist filter; matched watchlisted films return `on_watchlist: true`. The prop drives button state.
- **`/wrapup` slash command** at `.claude/commands/wrapup.md` (local only, untracked).
- **iOS splash assets** baked into home-screen install. PR #66 fix only takes effect after remove + re-add.

## Remote

Private repo at [rexnowacki/film-goblin](https://github.com/rexnowacki/film-goblin). `origin/master` is default. Deployed to Vercel as `film-goblin` (skulldrinker team) at https://film-goblin.vercel.app. `.vercel/project.json` checked in at repo root.

**Deploy rule: always `npx vercel deploy --prod --yes` from the repo root.** See Gotchas below.

## Git workflow

Two machines, same codebase, same protocol.

**Always-on:**
- Never commit to `master` directly. Feature branches only.
- Open PRs; don't merge directly.
- Commit frequently; push when work pauses.

**Stay in sync with `git fetch`** — the other machine may have merged since you last looked.

- **Before starting a branch:** `git fetch origin && git checkout master && git merge --ff-only origin/master`
- **Before pushing a long-held branch:** `git fetch origin`; if `origin/master` moved, `git rebase origin/master`
- **Before deploying:** `git fetch origin` to confirm you're shipping current master
- Prefer `git fetch` + explicit `merge --ff-only` / `rebase` over `git pull`

**Collision hot spots:**
- `CLAUDE.md` "Current state" — both `/wrapup` runs edit the same paragraphs
- `app/lib/supabase/types.ts` — hand-edited; regen on the other machine will clobber. Commit type edits in their own PR.
- `db/migrations/0NNN_*.sql` — numbered sequentially. If both devs add the same number, second-to-merge renumbers.

## Packages

Six packages, deployed/run independently. Each has its own `CLAUDE.md`.

| Package | What it is | CLAUDE.md |
|---------|-----------|-----------|
| `app/` | Production Next.js 15 app. UI, auth, server actions, API routes, cron. | `app/CLAUDE.md` |
| `worker/` | Price-tracking worker. Polls iTunes, writes price_history, emits alerts. | `worker/CLAUDE.md` |
| `db/` | Schema package. All migrations, RLS, triggers, DB tests. | `db/CLAUDE.md` |
| `notifier/` | Email digests via Resend. Library only — no CLI. | `notifier/CLAUDE.md` |
| `fg-trailers/` | Rust TUI for curating trailer URLs. Local admin tool. | — |
| `src/` | Legacy Vite+React design prototype. Read-only reference; production = `app/`. | — |

Routing shorthand: UI → `app/`. Prices/iTunes → `worker/` + app cron. Schema/RLS → `db/migrations/`. Email → `notifier/`.

## Node version

Node 20 required. Repo pins via `.nvmrc` but system default is often Node 16. Before any `npm`/`tsx`/`node` call:
- `nvm use 20`, OR prefix: `PATH=$HOME/.nvm/versions/node/v20.20.2/bin:$PATH`

Background/parallel bash calls don't share shell state — use the PATH prefix for one-shots.

## Sub-CLAUDE.md index

Layer-specific rules, patterns, and gotchas live in the package sub-files. Read the relevant one before working in that area.

| File | What it covers |
|------|---------------|
| `app/CLAUDE.md` | Deploy rule, Node 20, route structure, iOS PWA rules, env vars |
| `app/components/CLAUDE.md` | `"use client"`, `as never` cast, responsive helpers, design system, button conventions |
| `app/lib/actions/CLAUDE.md` | `_private`/`public` split, auth guard, `revalidatePath`, ProfileFields spread, test template |
| `app/lib/queries/CLAUDE.md` | Client injection pattern, `films_with_stats`, PostgREST embed gotcha, coven bidirectional check |
| `app/lib/queries/fyp/CLAUDE.md` | FYP v3 design, band system, cold-start, all tuning constants |
| `app/lib/supabase/CLAUDE.md` | Which factory to use, service-role guard, `types.ts` regen procedure |
| `app/lib/theaters/CLAUDE.md` | Scraper file map, adding a provider, cron trigger |
| `db/CLAUDE.md` | Two migrate commands warning, pg-mem strip list, RLS test shape, `bond()` helper |
| `db/migrations/CLAUDE.md` | Feature → migration range map, how to add a migration, prod apply procedure |
| `worker/CLAUDE.md` | Module responsibilities, NUMERIC/BIGINT coercion, no-raw-SQL rule, re-export contract |
| `notifier/CLAUDE.md` | Public API contract, sender domain status |

## Gotchas

These are global gotchas that don't belong to any single package. Package-specific gotchas live in the sub-CLAUDE.md files above.

- **`git commit -m` heredocs intermittently mangle the message** — commits land with subject `"Error:  does not exist."` when using `$(cat <<'EOF' ... EOF)`. Workaround: `Write` to `/tmp/msg.txt`, then `git commit -F /tmp/msg.txt`. `--amend -F` from the same file fixes a mangled message without losing the tree.

- **Vercel deploy must run from the repo root.** The Vercel CLI resolves `.vercel/project.json` from CWD — does NOT walk up. The project has `rootDirectory: app` set in the Vercel dashboard, so deploying from repo root is correct.
  - Deploying from `app/` silently creates a new garbage project named after CWD. Delete with `npx vercel project rm <name>`.
  - For worktrees: copy root `.vercel/project.json` into the worktree root, then deploy from the worktree root — NOT `<worktree>/app/`.

- **Supabase prod DB via session-mode pooler.** Direct connection (`db.<project>.supabase.co:5432`) is IPv6-only and unreachable from this machine. Use `aws-1-us-west-1.pooler.supabase.com:5432` with user `postgres.<project-ref>`. Connection string + password in `passwords.txt` at repo root (gitignored). Source env before `npm run migrate`: `set -a; source app/.env.local; set +a`.

- **Worktrees live under `.worktrees/`** (gitignored). The `superpowers:finishing-a-development-branch` skill cleans them up on merge.

- **`BRAVE_SEARCH_API_KEY`** — used only by `app/lib/actions/admin/apple-tv-search.ts`. Rotate: regen at brave.com/search/api/ → `npx vercel env rm BRAVE_SEARCH_API_KEY <env>` + `add` for each env → update `app/.env.local` → redeploy.

## Production stack

Committed direction lives in **`docs/superpowers/stack.md`** — read before proposing tech choices, update there (not here).

## Sub-project history

Full table at **`docs/sub-project-history.md`**. Specs under `docs/superpowers/specs/`, plans under `docs/superpowers/plans/`. Read the spec when working on a related area. New rows appended as sub-projects ship.

## Queued sub-projects

Nothing queued. See `docs/roadmap.md` for prioritized candidates.
