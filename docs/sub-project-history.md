# Sub-project history

Complete sub-projects, in order. Each one has a spec under `docs/superpowers/specs/` (and usually a plan under `docs/superpowers/plans/`) — those are the canonical record of design decisions. Read the spec when working on a related area.

`CLAUDE.md` no longer carries this table inline; it links here. New rows get appended to the end as sub-projects ship.

| # | Name | Spec |
|---|---|---|
| 1 | Apple data source — `worker/` package | `2026-04-20-apple-data-source-design.md` |
| 2 | Database schema + RLS — migrations `0100`–`0117` | `2026-04-21-schema-rls-design.md` |
| 3 | Next.js scaffold + auth + UI port — `app/` | `2026-04-21-nextjs-app-design.md` |
| 4 | Worker HTTP mount (Vercel Cron) | `2026-04-22-worker-cron-mount-design.md` |
| 5 | Notifications pipeline — `notifier/` + Resend | `2026-04-22-notifications-pipeline-design.md` |
| 6 | Social — coven, follows, recommendations, activity | `2026-04-23-social-features-design.md` |
| 7 | Auth polish — password reset, email verification, Google sign-in | `2026-04-23-auth-polish-design.md` |
| 8 | Mobile responsive (Tier-A) — 720px breakpoint, hamburger nav | `2026-04-23-mobile-responsive-design.md` |
| 9 | Mobile polish — filter rows, `.h-display` tuning, `.check-zine`, font-usage rule | `2026-04-23-mobile-polish-design.md` |
| 10 | Coven feed hearts (A) — `activity_reactions`, `HeartButton`, `LikersBottomSheet`, universal heart | `2026-04-24-coven-feed-hearts-design.md` |
| 11 | Discovery chrome polish (B1) — chip-row sort on `/films`, dropped Chapter II eyebrow, installable PWA shell | `2026-04-25-discovery-chrome-polish-design.md` |
| 12 | Library — Owned (C1) — `library` table + RLS, `/library`, `OwnedButton` + `FilmActions`, auto-watchlist-cleanup, `films_with_stats.owned_count` | `2026-04-25-library-owned-design.md` |
| 13 | Activity Feed Grouping (D1 / #52) — read-time `groupFeed`, `FeedItem` discriminated union, `ActivityWatchlistAddedGroup`, 30-min window + 24-hr ceiling + min-3 size, watchlist-adds only in v1 | `2026-04-25-activity-feed-grouping-design.md` |
| 14 | Watched Action (C2) — `watched` event-stream + `/watched` (stats hero + month-grouped diary), `WatchModal`, `WatchedButton`, `watch_logged` activity kind + trigger + groupFeed registration, `broadcast_watched` toggle | `2026-04-25-watched-action-design.md` |
| 15 | Social signal on posters (B2) — `films_with_stats.watcher_count`; `FilmPoster` opt-in `watchlistCount`/`watcherCount` w/ corner pill; `/films` badges; `/film/[id]` hero caption | `2026-04-25-social-signal-posters-design.md` |
| 16 | Covenfolk merge — unified `/people` + `/coven` into one page at `/coven`. Pending-invites strip + two-pane "Your Coven \| Find People" body. Inline four-state invite button. New `getRelationshipMap`, `excludeUserIds` arg on `getProfilesBySearch`, new `SearchPersonRow`. `/people` redirects. No schema. | `2026-04-26-covenfolk-merge-design.md` |
| 17 | Activity comments — flat 140-char comments on single-event rows. New `activity_comments` (0129) + `comment_on_activity` notification kind + trigger (0130/0131). `CommentButton` + `ActivityCommentThread` w/ optimistic insert/delete, "Hide"/"Delete" pills. Bell rows deep-link to `/home?activity=<id>` w/ auto-expand. | `2026-04-27-activity-comments-design.md` |
| 18 | Watchlist redesign — collapsed row layout to a poster grid like `/library`. New `PosterDropBadge` (≥10% threshold) + caption-line "Apple TV · $9.99 →". `WatchlistSortChips` replaced native `<select>`. `WatchlistRow.tsx`, `WatchlistSortSelect.tsx`, ~110 lines `.watchlist-row*` CSS deleted. On-poster buy pill tried then removed; price folded into caption link. No migration. | `2026-04-29-watchlist-redesign-design.md` |
| 19 | Onboarding redesign — collapsed 723-line five-chapter ritual into a single-page form (handle, threshold slider, pick three films). Hero matches `/library`/`/watchlist`. Chapter labels, progress bar, blood-pact certificate, signature flourish — all gone. `OnboardingPayload` 7→3 fields (genres/storefronts/follow-seeding dropped). Films query server-rendered. No migration. | `2026-04-29-onboarding-redesign-design.md` |
| 20 | Identity at signup — handle + display_name on signup form; trigger reads `raw_user_meta_data` (mig 0136) w/ email-derived fallback for OAuth. `signUp` validates both, pre-checks handle uniqueness via `serviceRoleClient`. Handle auto-suggests from display_name and locks on manual edit. `OnboardingForm` pre-fills handle. Structural fix for the "teeth tony" 404 bug class. | `2026-04-29-identity-at-signup-design.md` |
| 21 | Username login + optional email at signup — PRs #62 (mechanical rename `handle`→`username` across ~64 files) + #63 (auth changes). Migs `0137` (rename + trigger update) + `0138` (`email_added_at` + email-change trigger). Synthetic `<username>@noreply.film-goblin.app` via `auth.admin.createUser({ email_confirm: true })`. Signin sniffs `@`. /settings "Add Email"/"Update Email" via new `updateEmail` action. Notifier filters on `email_added_at IS NOT NULL`. Dashboard "Confirm email" toggle stays ON for email-CHANGE confirmation. PR #65 dropped display name from signup too. | `2026-04-30-username-and-optional-email-design.md` |
| 22 | Per-kind email toggles — mig `0139` adds 4 bool cols (price drops, coven recs, comments, coven invites), backfill `email_price_drops` from `email_notifications_enabled`. /settings "Email me when…" subsection (only price drops actually send today). Notifier swap to `email_price_drops`. Unsubscribe sets all four FALSE. Token rotation on re-enable from fully-opted-out. `email_notifications_enabled` kept for compat. | (no spec) |
| 23 | User roles + badges (goblin / witch / high goblin) — mig `0140` adds `profiles.role` w/ CHECK; tightened RLS UPDATE to forbid client role changes. New `<RoleBadge />` (inline-SVG glyphs) on `/p/[username]` next to display_name h1, 28px. /admin/users/[id] Role section auto-toggles staff row. /admin/users list shows tiny role pill. high_goblin dormant — no billing yet. | (no spec) |
| 24 | Coven rating — RT-style binary recommend per watch. Mig `0141` adds `watched.recommended BOOLEAN NULL`; extends `films_with_stats` w/ `coven_rating_count` + `coven_rating_pct` (latest-per-user dedup, NULL until count≥5). WatchModal verdict pill pair. `<CovenScore />` on `/film/[id]` w/ tiers (Anointed/Coven approved/Coven divided/Cursed). Mig `0142` writes `recommended` into `watch_logged` activity payload. Mig `0143` backfilled the 4 existing rated watches via ±5s join. | (no spec) |
| 25 | Comment sheet polish + likes — mig `0147` adds `activity_comment_reactions` (composite PK `(user_id, comment_id)`, `TO authenticated` policies, `acr_bump_count()` trigger w/ `SECURITY DEFINER` maintaining `like_count` on `activity_comments`). New `toggleCommentReaction` action mirrors `toggleReaction`. Restyled `CommentSheet` to prototype 1: serif "Comments • N" header, 36px avatars, stacked username/timestamp/body, heart+count column, inline muted "Delete" text-link, viewer-avatar + rounded-pill composer w/ inline `N/140` counter and smart Post (text-link disabled / solid pink pill enabled). `BottomSheet.title` widened to ReactNode. `HeartIcon` extracted to shared component. `CommentItem` gains `like_count` + `liked_by_me`; `getCommentSummariesForActivities` takes a `viewerId` arg. | `2026-05-01-comment-sheet-polish-and-likes-design.md` |
| 26 | Username on utility surfaces — flipped 37 render sites from `display_name ?? username` to bare `username` across 17 files (10 activity components + LikersBottomSheet, SearchPersonRow, TopNavChrome, both notification rows, /coven, /p/[username] coven chips). `/p/[username]` h1 + main avatar still use `display_name ?? username`; /settings Display name input still editable; admin surfaces unchanged. No schema, no migration. | `2026-05-01-username-on-utility-surfaces-design.md` |
| 27 | `like_on_comment` notification — migs `0148` (enum + `notify_comment_likes` opt-out) + `0149` (`notify_like_on_comment` SECURITY DEFINER trigger on `activity_comment_reactions`). Recipient = comment author; self-likes filtered; opt-out skips the INSERT entirely. Bell row mirrors `comment_on_activity` payload + copy. Smarter grouping: `like_on_comment` keys on `payload.comment_id` (not actor) with threshold 2 — single liker reads "<liker> liked your comment", 2+ reads "N people liked your comment". /settings adds "Notify me when someone likes my comment" checkbox. | `2026-05-01-like-on-comment-notification-design.md` |
| 28 | Modal visual unification — `RecommendModal` converts to `BottomSheet` (drops the rotated zine-card overlay; ritual copy "Cast the Rune • {filmTitle}" moves into the sheet header). `AvatarEditor` keeps its centered fullscreen overlay but adopts the dark `#141414` + accent top border + serif title visual language. Form inputs retoned for dark panels; Submit unified to `.btn` solid-pink. Bundled fix: RecommendModal sent-state now resets on close. | `2026-05-01-modal-visual-unification-design.md` |
| 29 | RecommendModal picker — replace native `<select>` with in-sheet UI: search input + horizontal "top covenfolk" avatar chips (people the user has recommended to most often, computed from `activity` rows of `kind = 'recommendation_sent'`) + a substring-filtered list of remaining covenfolk that appears as the user types. Selection lifted to component state; submit disabled until a member is picked. New query `getTopRecommendedCovenMemberIds`; new pure-function `filterCovenMembers` extracted for testability. Falls back to alphabetical chips for users with no recommendation history. | `2026-05-01-recommend-modal-picker-design.md` |
| 30 | Sticky invite CTA + auto-coven-request on signup — anon viewers of `/p/[username]` see a sticky "@&lt;inviter&gt; invited you to Film Goblin" banner with a Sign up button linking to `/auth/signup?invite=&lt;inviter&gt;`. Signup form threads the param to a 1-hour HttpOnly `fg_invite` cookie via the `signUp` action. `_completeOnboarding` reads the cookie and inserts a `coven_request` (inviter → new user) via service-role; the existing `notify_coven_request_pending` trigger fires the `coven_invite_pending` bell row automatically. Idempotent (UNIQUE constraint + helper pre-checks for existing bonds + opposite-direction requests). Connection is one accept-tap from forming. | `2026-05-01-sticky-invite-cta-design.md` |
| 31 | Film social meta + share button + sticky CTA — `generateMetadata` on `/film/[id]` returns OG/Twitter card meta with iTunes artwork as `og:image`, so any shared film URL unfurls into a poster card in iMessage / Slack / Discord / Twitter. New `ShareFilmButton` (Web Share + clipboard fallback) constructs URLs as `/film/<id>?from=<viewer-username>` when logged in. Page reads `?from=`, calls new service-role helper `getSharerWatchForFilm` (deliberately bypasses `broadcast_watched` because the share tap is the consent signal), renders `SharerWatchPin` with the sharer's most recent watch + note + verdict above the hero. Anon viewers also see `FilmCTABanner` — copy adapts to whether `?from=` is present, and when present the Sign up link threads `/auth/signup?invite=<from>` so the existing #30 invite-cookie flow auto-creates a coven request on signup. End-to-end: SMS share → iMessage poster card → tap → film page with sharer's pin + CTA → sign up → onboarding → coven request from sharer waiting. | `2026-05-01-film-social-meta-share-design.md` |
| 32 | Tag content infrastructure (sub-project A of tagging) — mig `0151` adds `tags` + `film_tags` (composite PK, public read, no client write) and seeds the canonical 18 horror sub-genres + 36 vibes. New `setFilmTags` admin action (delete-then-insert replace, validates types and ≤3 vibes, gated by `requireAdmin`). New `<FilmTagEditor>` chip-picker on `/admin/films/[id]/edit`. New "Untagged only" filter chip on `/admin/films`. Replaces the demo `FilmTagsRow` on `/film/[id]` with real data from `getFilmTags`. Director continues to live on `films.director`. Films start untagged; admin curates over time. FYP recommender (sub-project B) builds on top of this. | `2026-05-01-tag-content-infrastructure-design.md` |
| 33 | Tagging system v2 — replaces #32's two-facet system with the seven-facet positional system from the v2 staff style guide. Mig `0152` truncates v1 + reseeds 88 canonical tags (24 sub-genre + 17 subject + 16 tone + 21 theme incl. `breakup horror` + 6 setting + 4 content), expands `tags.type` CHECK to six values, adds `film_tags.position SMALLINT` + `is_primary BOOLEAN` with partial unique idx `(film_id) WHERE is_primary = TRUE`, adds `films.horror_adjacent BOOLEAN`. `setFilmTags` rewritten with 14 distinct validation paths (per-facet caps, "Secondary in tail" rule, type defense, ordered-set equality, "exactly 1 Primary, must be subgenre"). Service-role transaction: delete-then-insert film_tags at positions 1..N + update films.horror_adjacent (= primary subgenre name is `'thriller'`). New two-stage `<FilmTagEditor>` on `/admin/films/[id]/edit`: chip-group picker (six facets w/ per-facet caps) → `@dnd-kit/sortable` drag-to-reorder list with Primary locked at slot 1, virtual director row locked at slot 2 (from `films.director`), dashed accent divider between slots 5 and 6 reading "visible above · hidden below". `<FilmTagsRow>` rewritten on `/film/[id]` to render the 5-slot capsule from new `{visible, hidden}` shape. `/admin/films` "Untagged only" filter simplified — v2 untagged = zero rows in `film_tags` (any tagged film has ≥1 row from the Primary requirement). Hidden tail (positions 5+) returned by `getFilmTags` so it can feed FYP recommender (sub-project B, still deferred). | `2026-05-02-tagging-system-v2-design.md` |
| 34 | Coven page chip rework — replaces `/coven` left-pane card grid with a top-4 chip row + search + "See all (N)" BottomSheet. New `getRankedCovenfolk` query scores covenfolk by 90-day interaction (`recommendation_sent` + reactions on their activity + comments on their activity, equal weights, alphabetical tie-break). New `<CovenChipRow>` client component handles chips + search + inline filtered results + see-all trigger; reuses `filterCovenMembers` from #29. New `<CovenSeeAllSheet>` BottomSheet shows full ranked roster scrollable, same score order. Compact rows (avatar + username + Leave) shared between search results and the see-all modal. Chips navigate to `/p/<username>`. Empty (0) → italic copy. Small (1–4) → chips only. Large (5+) → chips + search + see-all. "Find People" right pane unchanged. No schema, no migration. | `2026-05-02-coven-page-chip-rework-design.md` |
| 35 | FYP recommender — new `/for-you` route ranks the catalog by personal affinity. Mig `0154` adds `profiles.lane_tag_ids UUID[]` + `films.editorial_starter BOOLEAN`. Affinity built from 6 weighted user signals (`+3` watch+liked / `+2.5` recommendation_sent / `+1.5` library_added / `+0.75` watchlist_added / `+0.20` reaction / `-4` watch+disliked) × per-facet tag multipliers (Primary subgenre 3.0 down to Content 0.5), per-tag floor at 0. Cold start is 4 additive layers — editorial starter pack (20 hand-curated films, true cold) → coven-borrowed × 0.3 (any bond, weighted by #34's interaction score) → lanes × 1.5 per picked tag → own behavior. `<ForYouFeed>` infinite-scroll mirrors `/home`'s IO + Load-more pattern. Each row gets a tiny italic "why" caption ("matches your folk horror affinity" / "highly rated by your coven" / etc — 5 reason kinds). New `/tags/[name]` listing pages — every `<FilmTagsRow>` pill (excluding director) links to one. New LanePicker section on `/settings` lets users seed sub-genre / tone / theme lanes. Pure-function `scoreFilms` + `getUserOwnAffinity` + composer all unit-tested with mocks (53 specs). On-demand affinity computation v1; cache seams documented at `getUserAffinity` / `getCovenBorrowedAffinity` for mid-scale. `horror_adjacent` filtering deferred. | `2026-05-02-fyp-recommender-design.md` |
| 36 | Recommender v2 — algorithmic rebalance + calibrated percentage. Six layered improvements over #35: (1) **TF-IDF on tags** computed per-request from the candidate pool, so distinctive tags (`breakup horror`, `kaiju`) outweigh near-universal ones (`atmospheric`, `bleak`); (2) **affinity cap** of 30 prevents one runaway tag from dominating the vector; (3) **visible-tag boost** of 1.3× for `film_tags.position ≤ 4` honors the staff guide's editorial capsule; (4) **time decay** at half-life 1 year (`0.5^years`) so recent signals dominate ancient history; (5) **cosine-weighted coven borrowing** — close-taste mates contribute more than orthogonal ones, with cold-start fallback to interaction-score weighting; (6) **verdict-anchored calibration** maps raw scores to `(score − meanDisliked) / (meanLiked − meanDisliked) → 0..100` so the percentage is stable across catalog growth. New `<MatchPill>` component overlays each `/for-you` poster with the calibrated % (or verbal fallback "strong match" / "your kind" / "interesting pick" / "outside your lane" when `totalRatings < 3`). New `app/lib/queries/fyp/calibration.ts` (pure-function, 23 specs). Total test count across recommender modules: 80 unit specs, all pass. No schema changes. | `2026-05-02-recommender-v2-design.md` |
| 37 | Recommender v3 — math-review fixes. External math expert reviewed `algo.md` and flagged five concerns. Five corresponding patches landed: (1) **Drop μ at scoring time** — facet multiplier was being applied at both affinity-construction AND scoring (compounding to μ², so Primary subgenre matches were 36× content matches). Now applied only at construction; scoring is `score = Σ aff(t) × idf(t) × β(t,F) + B_coven`. (2) **Length penalty γ=0.5** — divide raw score by `√|tags(F)|` so heavily-tagged films don't accumulate raw advantage from breadth. (3) **Smoothed + clamped IDF** — `clamp(log(1 + N/(1+df)), 0.75, 3.0)` replaces raw `log(N/df)` to dampen extremes on small catalogs. (4) **Aversion vector** — new `getUserAversion()` parallel to own-affinity, accumulating dislike magnitude with the same time-decay + facet-multiplier + cap pattern. Scoring now subtracts `λ=0.8 × aversion-mass` from positive score, so explicitly-disliked tags actively push films down (not just zero out positive matches). (5) **Score bands replace % pill** — "94%" implied probability we couldn't validate; replaced with rank-percentile bands (Hexed for You / Strong Match / Good Omen / Strange Pull) plus a separate Coven Favorite badge. Calibration helper unwired from the display path (preserved for future use); circularity concern sidestepped because bands depend on rank, not anchored percentages. 92 unit specs pass. No schema changes. | `2026-05-02-recommender-v2-design.md` (delta documented in `algo.md`) |
| 38 | Film page price sticker — die-cut vinyl price sticker in the `/film/[id]` hero text column (accent bg, void border, offset shadow, rotate(-6deg), 2px radius — OG-card language). Context beat from 180-day history via new pure helper `pickPriceBeat` (`app/lib/price-beat.ts`, 8 unit tests): lowest-in-window / ▼ down-from-$X / "on Apple TV". Not tappable; Buy button label simplified to drop the price. New `FilmPriceSticker.tsx`. On-poster placement rejected again (PR #51 settled). No migration. | `2026-07-07-film-page-price-sticker-design.md` |
| 39 | The Claiming — buy-click capture & purchase confirmation. New `BuyOnAppleLink` client component arms a `localStorage` pending-buy queue (`fg_pending_buys`, pure module `app/lib/purchase/pending.ts`, 10 unit tests) at both buy surfaces (film page + watchlist caption), signed-in only. `PurchasePrompt` modal (mounted once in the signed-in layout) checks the queue on mount + tab-refocus, asks "Did you claim **{title}**?" once an entry is 2min–48h old; declines clear, dismisses defer once then drop. New `confirmPurchase` server action fills `library.price_paid_usd` (mig 0211, nullable `NUMERIC(6,2)`) — fresh add stores price + clears any watchlist row; existing row only fills a NULL price, never overwrites. `/library` gains a Claimed/Tithed/Saved stat strip via `getLibrarySavings` (savings = peak `price_history` price − paid, computed at read time, never stored). **Code-review catch:** `library` had no UPDATE policy/grant since mig 0122 (only SELECT/INSERT/DELETE) — the fill-null path would have thrown permission-denied; mig 0211 amended pre-apply with an owner-only `library_update` policy + `GRANT UPDATE`. `price_paid_usd` is coven-visible alongside the row (accepted, no column guard). Device smoke test owed (see CLAUDE.md Open threads). | `2026-07-07-buy-claim-loop-design.md` |
| 40 | FROM THE PIT — three-tier feed treatment + seal avatar. System feed events now derive one of three tiers (whisper/standard/full) purely from event type via `getPitTier`/`getPitKicker`/`getPitPriceVars`/`getPitBadges` (new `app/lib/feed-events/tier.ts`, 11 unit tests) — full reserved for `all_time_low` only (the sole type with genuine before/after price data). New feed-level `resolvePitTiers` (`pitCadence.ts`, 8 unit tests) caps full cards at one per 8-item sliding window, demoting excess candidates rather than dropping them. New seal avatar `PitSeal` (replacing the same-day "FG" SVG `PitSigil`) at 40px on `/home`, 32px on the landing card — real artwork still owed by the project owner, ships with a 1×1 placeholder. Fixed (non-theme-switched) `--pit-*` tokens in `00-core.css`. 4 copy templates rewritten for a goblin-action-first voice rule (forward-only). `LandingFeedRow`'s system variant widened from `{copy, film}` to `{event}` to make tier resolution possible on the landing page. Two real typecheck gaps beyond the plan fixed: `FeedRow.tsx` (a second, unreachable `SystemEventRow` consumer) and `FeedTabs`'s `composed`/`composedRaw` split (the array had already collapsed past the shape `resolvePitTiers` needs). Mobile screenshot deferred (no browser extension in the implementing session) — flagged as an open thread. No migration. | `2026-07-07-pit-tier-system-design.md` |
| 41 | FROM THE PIT — cadence caps. First of five sub-projects sketched from owner feedback that the Pit felt like a "deals board" competing with social activity. New `pit_impressions` table (mig 0212, mirrors `fyp_impressions`: SELECT-only RLS, all writes via `SECURITY DEFINER` RPC `record_pit_impressions`, `ON CONFLICT DO NOTHING`, batch cap 10) backs both permanent "already seen" exclusion and a daily counter. `getEligiblePitEventsForUser` (`pitSelection.ts`, 6 pure + 4 integration tests) caps new Pit events at 3/user/UTC-day and boosts watchlist matches +1000 on the 10–100 priority scale so they always win a slot. New `enforcePitPositionRules` (`pitPosition.ts`, 9 unit tests) runs between the untouched `composeFeed`/`resolvePitTiers` — drops (never reorders) items violating a 1-in-first-6 cap or a 2-user-item min-gap between any two Pit items, uniform across tiers. Impressions recorded client-side via `recordPitImpressions` (mirrors `recordFypImpressions`) on `SystemEventRow` mount. Signed-in `/home` only; anonymous landing page and signed-out `/home` untouched. Two real gaps caught during implementation: a test differentiator that used `PIT_DAILY_CAP` fresh impressions (indistinguishable from buggy behavior) tightened to `PIT_DAILY_CAP - 1`; `types.ts` needed a `Functions` entry for the RPC, not just the `Tables` entry. Aging/TTL, digest events, a Ledger page, and a Pit archive tab remain backlog. | `2026-07-08-pit-cadence-caps-design.md` |
| 42 | FROM THE PIT — aging / TTL. Sub-project #2 of 5. System feed events get a time-to-live on the signed-in `/home` feed so stale ones age out instead of resurfacing when the feed is quiet. New pure module `app/lib/feed-events/pitAge.ts` (14 unit tests): `classifyPitEventAge` (fresh <24h / aging <48h / stale ≥48h, rolling hours from `created_at`, upper-bound-exclusive) + `filterPitByAge` (fresh always kept, stale always dropped, aging kept only if the event's film is watchlisted; null/non-watchlisted films get a hard 24h life). Constants `PIT_FRESH_HOURS=24`/`PIT_AGING_HOURS=48`. Spliced into `getEligiblePitEventsForUser` as a 4-line insertion (age-filtered `fresh` feeds `rankPitCandidatesByWatchlist`, not raw candidates), +3 integration tests backdating `created_at`. Filter lives ONLY in `pitSelection.ts` — NOT in the shared `getRecentSystemEvents`, which also feeds the un-aged anonymous landing page. Rolling-hours boundaries (vs #1's UTC-calendar-day budget). "Don't resurface unless status changed" needed no code — a status change is a new row with a new id, already eligible under #1's permanent exclusion. Events never deleted; they persist in `feed_events` for the deferred Ledger (#4) / Pit tab (#5) to query un-aged. No migration, no new table, no UI change. | `2026-07-09-pit-aging-ttl-design.md` |
| 43 | FROM THE PIT — archive tab (#5 of 5; row written post-hoc from spec + merged diff, PR #195). Fourth `pit` tab on `/home`'s `FeedTabs` (all/coven/recs/pit): everything the goblin ever found, reverse-chronological, bypassing the entire #1/#2 cadence pipeline (no daily cap, no seen-exclusion, no age filter, no position rules). New `getPitArchiveEvents` (cursor = `created_at`, initial 30 + "Load more" server action `app/lib/actions/pit-archive.ts`), `app/lib/feed-events/pitArchive.ts` maps all 15 event types to client-side filter chips (Everything/Deals/Free/Catalog/Hauntings), new `PitArchiveTab.tsx`. `resolvePitTiers` runs (ATLs still full cards); `enforcePitPositionRules` doesn't. Critical: `SystemEventRow` gained `recordImpression?: boolean` (default true; pit tab passes false) — without it one archive scroll would permanently starve the main feed's Pit selection and burn the daily budget. Individual events only, never digests. No migration. | `2026-07-09-pit-archive-tab-design.md` |
| 44 | Film page price ledger (#4 of 5, as redefined by owner 2026-07-09 — the original "Ledger page" aggregation concept was retired, its global-deals role absorbed by the pit tab; row written post-hoc, PR #196). Quiet expandable logbook of price *changes* below `PriceStatBlock` on `/film/[id]`: "Jul 3 · dropped to $4.99 (was $14.99)". New pure `app/lib/price-ledger.ts` (`extractPriceChanges`: consecutive-capture comparison over the page's existing 180-day fetch, change-points only, first capture = "first sighted", SD price only, `is_sale` chip). New `FilmPriceLedger.tsx`; 5 recent + "Show full ledger" expander. Public. Stat block/sparkline untouched (PR #175 chart demotion holds). No migration, no new query. | `2026-07-09-film-price-ledger-design.md` |
| 45 | FROM THE PIT — digest events (#3 of 5; row written post-hoc, PR #197). Same-type non-personal Pit events bundle at READ time in `getEligiblePitEventsForUser` into one standard-tier digest card ("5 films are free tonight" + member poster chips + "See all →" → `/home?tab=pit`); `feed_events` stays one-event-one-row. New `app/lib/feed-events/pitDigest.ts`: surviving non-exempt events group by `event_type`, ≥2 (`DIGEST_MIN_SIZE`) become a synthetic `SystemFeedEvent` (key `digest:{type}:{date}:{member-sig}`) flowing through untouched `composeFeed`/`enforcePitPositionRules`/`resolvePitTiers`. Never digested: watchlist matches, `all_time_low`, `last_showing`. Digest copy rendered at read time — the one exception to frozen-copy. Mig 0214: `pit_impressions.digest_key` + `record_pit_impressions` re-created with defaulted second arg (DROP+CREATE — defaulted param = new PG signature); rendering records ALL members (none resurface) while the daily cap counts `DISTINCT COALESCE(digest_key, event_id)` — one digest = one slot. `DIGEST_MAX_MEMBERS = 10`; overflow stays eligible. Signed-in `/home` only. | `2026-07-09-pit-digest-events-design.md` |
| 46 | Return Rituals Phase 1 — behavioral instrumentation foundation. Mig 0215 adds owner-readable/RPC-only `product_events`; bounded 13-event registry, client-generated idempotency IDs, signed-in session tracker, fail-soft one-retry browser queue, 180-day recorded cleanup job, and read-only seven-day return-ritual report. PR #203 (`0975f11`) + production-smoke query-serialization fix PR #204 (`ee0f600`); migration-first rollout, archive-mode Vercel deploy. Phase 2 gated on signed-in smoke + seven baseline days. | `2026-07-10-return-rituals-design.md` |
| 47 | Return Rituals Phase 2 — visible social promise and explicit return contract. Mig 0216 adds owner-scoped deferrals; `/home` resolves at most one truthful next action with fail-closed reads and actual-view/action instrumentation, while onboarding and `/coven` state concrete social benefits. PR #206 (`a9ad29c`), migration-first deploy. Owner explicitly overrode the low-traffic sequencing wait; production later recorded 16 views and 4 actions across 2 users, below the sample needed for a rate claim. | `2026-07-10-return-rituals-design.md` |
| 48 | Return Rituals Phase 3 — explainable taste-twin discovery. Mig 0217 adds owner-scoped 90-day suppressions; bounded bulk ranking excludes existing/pending bonds in both directions, requires evidence, shows traits/shared films but never an uncalibrated score, and gates shared-taste pages by an accepted coven edge. PR #207 (`2039b83`), migration-first deploy; production report confirms the suppression schema is available. | `2026-07-10-return-rituals-design.md` |
| 49 | Return Rituals Phase 4 — gazing completion, home watch nights, reminders, aftermath, and post-success continuations. Migs 0218–0219 extend notification kinds and gazing state/RLS; explicit invitees or real-coven broadcasts can plan home watches, private locations stay participant-only, reminders dedupe, hosts close outcomes, attendees self-confirm, and continuation prompts offer at most two actions with session-close suppression. PR #208 (`0a26a3b`), migrations-first deploy `dpl_8Q9Nz4ryWntj8PA9aLxfNXLKc6rY`; app/DB/worker CI green and focused real-Postgres gazing proof 10/10. Authenticated two-account/device smoke remains owed. | `2026-07-10-return-rituals-design.md` |
| 50 | App performance + UI-state repair pass (PR #211, `72ed1db`). `/home` now launches activity, return-contract, Pit/archive, watchlist-price, ritual, system-event, viewer, and staff reads as one dependency-aware `Promise.all` instead of three serial waves; FYP coven borrowing reuses the in-flight own-affinity promise instead of querying it twice. Back remains available on fresh loads; notification read/clear state rolls back on server failure; push subscription transitions reuse existing browser subscriptions and undo partial failures. No migration. | (no spec) |
| 51 | Public profile redesign (PR #212, `beeb3c7`). `/p/[username]` rebuilt from the supplied leftmost mock-up: identity/summary treatment, count strip, bio/location/joined metadata, goblin-voiced Relics empty state, and responsive Watchlist/Watched/Reviews/Lists collection tabs via new `ProfileCollectionTabs`, `profile-page.ts`, and `260-profile.css`. No badge-award schema was invented; empty state is truthful. No migration. | (no spec) |
| 52 | Profile Coven roster containment (PR #213, `a674f48`). Replaces long usernames beneath circular avatars with compact avatar/name pills, caps the inline roster at eight, and adds a scrollable full-roster modal via `ProfileCovenRoster`; helper/test coverage locks the eight-person split. No migration. | (no spec) |
| 53 | Profile staff verification mark restored (PR #214, `49bb886`). Public profiles now read the canonical `staff` table and show the pink pentagram beside staff/admin identity, preserving the old Twitter-check-like signal without trusting profile metadata. No migration. | (no spec) |
| 54 | Daily Omen match pill removal (PR #193, `be3ba0a`). The rank-band "Hexed for You" pill no longer renders on the For You Daily Omen, where the hero's own label already communicates recommendation context. Scoring and shelf pills are otherwise unchanged. No migration. | (no spec) |
| 55 | NEXT IN THE PIT browsable queue (PR #215, `59811be`). Server resolution returns an ordered, deterministic, deduplicated stack of up to five existing-priority candidates; client supports visible wraparound previous/next controls, queue position, and mobile swipe without changing dismissal state. **Set aside** persists only the active candidate's deferral, optimistically removes it, advances immediately, and rolls back on failure; the tracker records viewed keys once. Regression coverage spans queue mechanics, resolver priority/dedupe, and query shape. No migration; deployed as `dpl_2rQto9qBFgZy2LERBGt4Gp9ar2vQ`. | (no spec) |
| 56 | iOS fast-scroll bottom-nav mitigation (PR #216, `bbf8cc3`). Cross-route fresh-load jumps were traced to the iOS 26/WebKit fixed-layer visual-viewport regression (exact half-screen trigger reported with Reduce Motion + Prefer Cross-Fade), not a profile/feed containing block. The existing mobile-only fixed bar is isolated in a `translate3d(0,0,0)` compositor layer with prefixed/unprefixed hidden backface; a red/green source-contract test preserves fixed/bottom/safe-area behavior and keeps the workaround inside `max-width:720px`. All CI green; production CSS verified after deploy `dpl_AhLdsv9VvynKdHz6edGps3MV6PRG`. Physical affected-iPhone proof remains owed. No migration. | (no spec) |
| 57 | Return Rituals polish (PR #218, `08770e9`). NEXT IN THE PIT now persists reviewed keys per user/UTC-day, resumes at first unseen, collapses after the final current card is left/acted on, and reopens for a new key; stale tabs and denied storage are covered. New authenticated `/coven/gazings` supplies Open/Aftermath sections with live role/roster state, account-menu + Coven entry points, and responsive cards. Cancellation is removed from index/feed/active reminders without deleting token/activity/RSVP history; RSVP/attendance UI and actions reject closed rows; terminal close and attendance serialize via scheduled-state CAS. Mig 0220 makes attendee mutations status-aware while preserving host-after-happened confirmation. Private theater bearer RSVP was repaired by materializing the authenticated token holder as an invitee before the viewer-scoped attendee write, without widening direct table access. App 713 tests + build/typecheck, DB 248 real-Postgres RLS/trigger tests, all CI green; migration-first deploy `dpl_33hLjduLU3UGVMz7x2b4PyqqDTxz`. Signed-in visual acceptance remains owed. | `2026-07-11-return-rituals-polish-design.md` |
| 58 | Mixed activity grouping repair (PR #220, `279f7cb`). `groupFeed` now groups interleaved `watch_logged`, `watchlist_added`, and `library_added` rows independently by kind within a contiguous actor block, preventing a watch-log run from consuming save rows as standalone skips. Existing actor, timing, and commented-activity boundaries remain; new `ActivityLibraryAddedGroup` renders expandable grimoire aggregates. Regression reproduces the production `el_dritch` burst. No migration; deployed as `dpl_6EhMNkwQsdxdoxxBz5EX4eDTFroc`. | (no spec) |
