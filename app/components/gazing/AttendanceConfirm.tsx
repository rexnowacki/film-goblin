"use client";import{useState,useTransition}from"react";import{confirmAttendance}from"@/lib/actions/gazing";import{trackProductEvent}from"@/lib/product-events/browser";import ContinuationPrompt from"@/components/continuations/ContinuationPrompt";
export default function AttendanceConfirm({token,inviteId,filmId,confirmed}:{token:string;inviteId:string;filmId:string|null;confirmed:boolean}){const[done,setDone]=useState(confirmed);const[justConfirmed,setJustConfirmed]=useState(false);const[pending,start]=useTransition();if(done)return <><span className="gazing-attended">Attendance confirmed.</span>{justConfirmed&&<ContinuationPrompt source="attendance_confirmed"filmId={filmId??undefined}/>}</>;return <button className="btn"disabled={pending}onClick={()=>start(async()=>{await confirmAttendance(token);trackProductEvent({event_name:"attendance_confirmed",subject_type:"gazing_invite",subject_id:inviteId,properties:{}});sessionStorage.setItem(`fg_continuation_pending:attendance_confirmed:${inviteId}`,"1");setDone(true);setJustConfirmed(true);})}>{pending?"Confirming…":"I was there"}</button>}
