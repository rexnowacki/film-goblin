# Tasks

Small in-repo tickets for cleanup and follow-up work. Status is encoded by
folder:

- `todo/` — queued, unclaimed
- `in-progress/` — currently being worked
- `done/` — completed archive

Each ticket is a standalone markdown file with front matter so parallel work
does not create a shared checklist conflict.
